# Leave VITE_API_URL empty/commented out so both local dev and production
# use relative /api path (same-domain requests via proxy/rewrites, bypassing CORS).
# VITE_API_URL=https://backend-alpha-one-57.vercel.app/api
 